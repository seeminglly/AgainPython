### 변수: 객체를 가리키는 것

> 변수이름 = 변수에 저장할 값
> 
- 자료형이 저장된 메모리의 주소를 변수가 가리킴
    
    > 메모리 주소 확인하는 방법a = [1,2,3]id(a)4303029896
    > 
- 리스트 복사
    
    > a = [1,2,3]b = a'a와 b는 완전히 동일'-> 확인하는 방법: a is b'True'
    > 
- 변수 값을 가져오면서 다른 주소를 가르키도록 복사하는 방법
1. [:]사용
    
    > a = [1,2,3]b = a[:] (a의 처음부터 끝까지 슬라이싱)
    > 
2. copy모듈: cpoy(a) 와 a[:] 동일
- 변수를 만드는 방법튜플(), 리스트[], 여러 개의 변수에 같은 값 대입(=) 가능
